.rating-stage
  .row
    .col-sm-3
      .badge.badge-user= t('show.rating.header')
    .col-sm-9
      - (1..7).each do |i|
        .col-custom
          .badge{ class: "badge-#{i}" } Стадия #{i}
      .col-total
        .badge Итог
- @users.each_with_index do |user, i|
  = render partial: 'user', locals: { user: user, number: i + 1 }


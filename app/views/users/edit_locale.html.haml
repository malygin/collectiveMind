%section.widget
  .body
    %h3=t('menu.locale_edit')

    -if @available_locales
      = select_tag 'select_set_locale', options_for_select(@available_locales, I18n.locale.to_s), class: "form-control", data: {project: "#{@project.id}", user: "#{current_user.id}"}

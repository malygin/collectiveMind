%section.widget
  .body
    %h3=t('menu.locale_edit')
    -if @available_locales
      = select_tag 'select_for_locales', options_for_select(@available_locales.collect{ |u| [u, u] }), class: "form-control"

    -#-if @available_locales
    -#  = select_tag 'select_for_locales', options_from_collection_for_select(@available_locales, "id", "content"),
    -#          prompt: t('form.select_locales'), class: "form-control"
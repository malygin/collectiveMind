- if boss?
	.scoreBar
		=link_to "+20 баллов",  "/users/"+@user.id.to_s+"/add_score/20", :remote => true, :method => :put, class: "voteCounter"
		|
		=link_to "+40 баллов",  "/users/"+@user.id.to_s+"/add_score/40", :remote => true, :method => :put, class: "voteCounter"
		|

.wholePost
	= render 'users/user_post', :user =>  @user
	.posts
		%span.namedate
			=  @user.name_title
			- if current_user == @user
				(это вы)
		%br
		


/ %br
/ %h1 Идеи
/ - unless @user.life_tape_posts.empty?
/ 	= render @user.life_tape_posts
/ -else
/ 	Пока не добавлено
/ %h1 Концепции
/ - unless @user.concept_posts.empty?
/ 	= render @user.concept_posts
/ %h1 Проекты
/ Пока не добавлено






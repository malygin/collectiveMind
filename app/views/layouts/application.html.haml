%html
  %head
    = render 'meta'

  %body
    #wrapper
      #header
        = render 'header'
        = render 'stages'
        = render 'top_menu'
      #middle
        #container
          #content
            - flash.each do |key, value|
              -if key == :success or key == :notice
                .success
                  =value
              -else 
                .error
                  =value
            = yield 
        #sideLeft.sidebar
          = render 'left_side'
        #sideRight.sidebar
          = render 'right_side' 
    #footer
      = render 'footer' 



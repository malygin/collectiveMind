%html
  %head
    =render 'meta'
  %body
    #wrapper
      #header
        =render 'header'
      #middle
        #container
          #content_wider
            - flash.each do |key, value|
              -if key == :success
                .success
                  =value
              -else 
                .error
                  =value
            = yield 
 
    #footer
      = render 'footer' 



.col-sm-4{id: "vote_aspect_#{vote_aspect.id}"}
  .vote_item
    .heading_cont
      %h6
        = vote_aspect.content
    .divider10
    .vote_controls.text-center
      = link_to vote_collect_info_post_path(@project, vote_aspect.id, status: 1), remote: true, method: :put do
        %button.btn.red.vote_button{:role => "button"}
          %i.fa.fa-exclamation
      = link_to vote_collect_info_post_path(@project, vote_aspect.id, status: 2), remote: true, method: :put do
        %button.btn.dark-grey.vote_button{:role => "button"}
          %i.fa.fa-minus
      = link_to vote_collect_info_post_path(@project, vote_aspect.id, status: 3), remote: true, method: :put do
        %button.btn.black.vote_button{:role => "button"}
          %i.fa.fa-times
    .divider15
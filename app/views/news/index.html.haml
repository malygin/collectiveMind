%section.widget.padding-increased
  %header
    %h3.text-center
      Обращения к пользователям
    - if current_user.boss?
      = link_to 'Добавить обращение', new_news_path, class: 'btn btn-success'
    %hr/

  .body.no-margin
    - @news.each do |news|
      %section.search-result
        %h4
          = news.title
        %p
          #{news.body[0..100]}...
        %p
          = link_to 'Подробнее', news_path(@project, news), id: "show_news_#{news.id}"

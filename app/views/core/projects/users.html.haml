.col-md-9
  %section.widget
    %header
      %h3.text-center=t('show.rating.title')

    .body
      .feed
        .wrapper
          .text-center
            = will_paginate @users
          #tabs-1
            %table.table
              %tbody
                - @users.each_with_index do |user, i|
                  = render partial: 'users/user_global', locals: {user: user, number: i+1}


          .text-center
            = will_paginate @users

.col-md-3
  %section.widget
    %header
      %h3 Фильтры

= form_for @frustration, :url => url do |f|
	= render 'shared/error_messages', :object => f.object
	%label 
		%b Что 
		вас не устраивает?
	%br
	%field
		= f.text_area :what
	%div{:class=>'timestamp'} - отрицательное действие, явление, установление, черта...

	%label 
		%b Когда 
		это происходит? 
	%br
	%field
		= f.text_area :when
	%div{:class=>'timestamp'} - во время какого события, мероприятия, процесса, ситуации...

	%label 
		%b Где? 
	%br
	%field
		= f.text_area :wherin
	%div{:class=>'timestamp'} -	место, организация, общность, в которой это происходит.	
	
				
	%actions
		= f.submit "",  class: "btn"
	%span
		.remaining  &nbsp;осталось
		.countdown
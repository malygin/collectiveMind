$("#plus_comment_#{@comment.id}").effect("highlight", {color: '#f5cecd'}, 1000);
$("#plus_comment_#{@comment.id}").find('span').html("#{@comment.useful ? 'Забрать баллы' : 'Выдать баллы'}");
$("#plus_comment_#{@comment.id}").find('span').toggleClass('text-success text-danger');

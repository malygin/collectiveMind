-if @vote
  -if @against == 'true'
    $("#minus_comment_#{@comment.id}").effect("highlight", {color: '#f5cecd'}, 1000);
    $("#minus_comment_#{@comment.id}").html(parseInt($("#minus_comment_#{@comment.id}").html())+1)
    $("#minus_comment_#{@comment.id}").click(function(e) { return false; });
  -else
    $("#plus_comment_#{@comment.id}").effect("highlight", {color: '#f5cecd'}, 1000);
    $("#plus_comment_#{@comment.id}").html(parseInt($("#plus_comment_#{@comment.id}").html())+1)
    $("#plus_comment_#{@comment.id}").click(function(e) { return false; });
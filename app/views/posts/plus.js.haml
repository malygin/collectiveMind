-#$("#plus_post_#{@post.id}").fadeIn(3000).fadeOut(3000);
-if @post
  -if @post.useful
    $('#plus_post_#{@post.id}').addClass('font_red')
  -else
    $('#plus_post_#{@post.id}').removeClass('font_red')

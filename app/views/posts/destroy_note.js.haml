$("#post_note_#{@post_note.id}").animate({height: 0, opacity:0}, 1000, function() { $(this).remove(); });
-if !(@post_aspect_note ? @post_aspect_note : @post).note_size?(@type.to_i)
  $("ul#note_form_#{@post_aspect_note ? @post_aspect_note.id : @post.id}_#{@type.to_i}").removeClass('border_note');
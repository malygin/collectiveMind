$('#comment_content_#{@comment.id}').html("#{escape_javascript( @comment.content )}");
$("#update_comment_#{@comment.id}").show();
$('#comment_#{@comment.id}').effect("highlight", {color: '#f5cecd'}, 1000);
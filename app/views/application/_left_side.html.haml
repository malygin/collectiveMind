%ul#side-nav.side-nav
  %li.panel#stage_life_tape{class:  @project.stage_style(:life_tape_posts) }

    -#=link_to "/project/#{@project.id}/life_tape/posts" ,  class: "stage_menu1" do
    -#  %i.fa.fa-edit
    -#  %span.name.h4.no-margin.text-left
    -#    1.
    -#    =@project.status_title(:life_tape_posts)
    - if request.original_url.include? 'life_tape'
      %a.stage_menu1.accordion-toggle.collapsed(data-toggle="collapse" data-parent="#side-nav" href="#life_tape")
        %i.fa.fa-edit
        %span.name.h4.no-margin.text-left
          1.
          =@project.status_title(:life_tape_posts)
      %ul#life_tape.panel-collapse.collapse
        -@project.aspects.each do |asp|
          %li{class:"#{'active' if asp == @aspect}"}
            = link_to "/project/#{@project.id}/life_tape/posts?asp=#{asp.id}" do
              %i.fa.fa-minus(style="font-size: 8px;")
              &nbsp;
              =" #{asp.content}"
              -#%b="(#{post.comments.count})"
    -else
      =link_to "/project/#{@project.id}/life_tape/posts" ,  class: "stage_menu1" do
        %i.fa.fa-edit
        %span.name.h4.no-margin
          1.
          =@project.status_title(:life_tape_posts)

  %li.panel#stage_discontent
    - if @project.status > 2
      -#= link_to "/project/#{@project.id}/discontent/posts", class:"stage_menu2" do
      -#  %h4.no-margin.text-left
      -#    2.
      -#    =@project.status_title(:discontent_posts)
      - if request.original_url.include? 'discontent'
        %a.stage_menu2.accordion-toggle.collapsed(data-toggle="collapse" data-parent="#side-nav" href="#discontent")
          %i.fa.fa-bar-chart-o
          %span.name.h4.no-margin.text-left
            2.
            =@project.status_title(:discontent_posts)

        %ul#discontent.panel-collapse.collapse
          -@project.proc_aspects.each do |asp|
            %li{class:"#{'active' if asp == @aspect_post}"}
              = link_to "/project/#{@project.id}/discontent/posts?asp=#{asp.id}" do
                %i.fa.fa-minus(style="font-size: 8px;")
                &nbsp;
                =asp.content
                %small="(#{asp.aspect_posts.count})"
                -#%b="(#{post.comments.count})"
      -else
        = link_to "/project/#{@project.id}/discontent/posts", class:"stage_menu2" do
          %i.fa.fa-bar-chart-o
          %span.name.h4.no-margin
            2.
            =@project.status_title(:discontent_posts)

    -else
      %a.stage_menu2
        %i.fa.fa-bar-chart-o
        %span.name.h4.no-margin(style='color:#444444')
          2.
          =@project.status_title(:discontent_posts)



  %li.panel#stage_concept
    - if  @project.status > 6
      -#= link_to "/project/#{@project.id}/concept/posts" , class:"stage_menu3" do
      -#  %h4.no-margin.text-left
      -#    3.
      -#    =@project.status_title(:concept_posts)
      - if request.original_url.include? 'concept'
        %a.stage_menu3.accordion-toggle.collapsed(data-toggle="collapse" data-parent="#side-nav" href="#concept")
          %i.fa.fa-magic
          %span.name.h4.no-margin.text-left
            3.
            =@project.status_title(:concept_posts)
        %ul#concept.panel-collapse.collapse
          -@project.proc_aspects.each do |asp|
            %li{class:"#{'active' if asp == @aspect_post}"}
              = link_to "/project/#{@project.id}/concept/posts?asp=#{asp.id}" do
                %i.fa.fa-minus(style="font-size: 8px;")
                &nbsp;
                =asp.content
                %small="(#{asp.aspect_posts.by_status(4).count})"
      -else
        = link_to "/project/#{@project.id}/concept/posts" , class:"stage_menu3" do
          %i.fa.fa-magic
          %span.name.h4.no-margin
            3.
            =@project.status_title(:concept_posts)
    -else
      %a.stage_menu3
        %i.fa.fa-magic
        %span.name.h4.no-margin(style='color:#444444')
          3.
          =@project.status_title(:concept_posts)


  %li.panel#stage_plan
    - if  @project.status > 8
      = link_to "/project/#{@project.id}/plan/posts" , class:"stage_menu4" do
        %i.fa.fa-table
        %span.name.h4.no-margin
          4.
          =@project.status_title(:plan_posts)
        %br
        %small.text-center=t('stages.plans', count: @project.plan_post.count)
    -else
      %a.stage_menu4
        %i.fa.fa-table
        %span.name.h4.no-margin(style='color:#444444')
          4.
          =@project.status_title(:plan_posts)


  %li.panel#stage_plan
    - if  @project.status > 9
      = link_to "/project/#{@project.id}/estimate/posts" , class:"stage_menu5" do
        %i.fa.fa-code-fork
        %span.name.h4.no-margin
          5.
          =@project.status_title(:estimate_posts)
        %br
        %small.text-center=t('stages.estimates', count: @project.estimate_posts.count)
    -else
      %a.stage_menu5
        %i.fa.fa-code-fork
        %span.name.h4.no-margin(style='color:#444444')
          5.
          =@project.status_title(:estimate_posts)

  %li.visible-xs
    =link_to t('menu.help'), help_posts_path(@project), style:'width:auto; margin-right: 10px;'

  %li.visible-xs
    =link_to t('menu.base_knowledge'),   knowbase_posts_path(@project) , style:'width:auto; margin-right: 10px;'

  %li.visible-xs
    =link_to t('menu.raiting_rc'), users_rc_users_path(@project), style:'width:auto; margin-right: 10px;'

  %li.visible-xs
    =link_to t('menu.raiting'), "/project/#{@project.id}/users" , style:'width:auto; margin-right: 10px;'

  %li.visible-xs
    =link_to "/project/#{@project.id}/journals" , style:'width:auto; margin-right: 10px;' do
      =t('menu.news')
  %li.visible-xs
    %a{:href => "/users/sign_out/", "data-method" => "delete"}
      %i.fa.fa-sign-out
      Выйти
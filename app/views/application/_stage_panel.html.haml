.subhead_navigate
  .container
    .col-md-8
      .border-block{ class: "#{'tooltip1' if name_controller == :aspect_posts}" }
        .btn-group
          - Core::Project::STAGES.each do |num_stage, stage|
            - if name_controller == stage[:type_stage]
              = link_to send("#{stage[:type_stage]}_path", @project.id), id: "go_to_#{stage[:type_stage]}", class: 'btn tooltip_b', 'data-html' => 'true',
              title: "#{num_stage}" + t("stage.open_#{@project.main_stage == num_stage}") + "#{stage[:description]}" do
                #{num_stage} СТАДИЯ: #{stage[:name]}
            - elsif @project.main_stage >= num_stage
              = link_to send("#{stage[:type_stage]}_path", @project.id), id: "go_to_#{stage[:type_stage]}",
                    class: 'btn tooltip_b', 'data-html' => 'true',
                    title: "#{num_stage}" + t("stage.open_#{@project.main_stage == num_stage}") + "#{stage[:description]}" do
                = num_stage
            - else
              = link_to '#', id: "go_to_#{stage[:type_stage]}", class: 'btn tooltip_b',
                    'data-html' => 'true', title: "#{num_stage}" + ' ' + 'стадия (недоступна) <br />' + "#{stage[:description]}" do
                = num_stage

    .col-md-4
      .border-block.help_icon.help_popover.tooltip1.tooltip_b#tooltip1_2{ class: "#{show_check_field?(current_stage_popover_status) ? 'shown_intro' : ''}",
          data: { project: "#{@project.id}", stage: "#{name_controller.to_s.gsub('_posts', '')}", status: "#{current_stage_popover_status}",
          text: t(current_stage_popover_status), trigger: 'manual' }, title: 'Читайте инструкции по работе на этом этапе стадии' }
      .border-block.progress_block
        = render partial: 'application/progress_bar'
    - if show_stage_subbar?

      .col-md-12
        .border-block.metro_block
          %ul.list-inline
            - Core::Project::STAGES[@project.main_stage][:substages].each do |num_sub, substage|
              %li.tooltip_b{ title: t("substage.status_#{substage_status_by num_sub}"), class: "#{'active theme_font_color' if @project.sub_stage == num_sub}" }
                %i.fa.fa-circle.fa-2x
                %a{ href: '#' }= substage[:name]

= link_to image_tag("logo.png", :alt => "Colleсtive mind", :class => "round", ), root_path
/ = @project.name
/ = image_tag("improve.png")    
- if signed_in?

  .greybut
    = link_to image_tag('edit.png'), "/project/#{current_user.projects.first.id}/users/#{current_user.id}/edit"
    = link_to current_user.name_title, "/project/#{current_user.projects.first.id}/users/#{current_user.id}", class: "username"
    = link_to " - Выйти", "/signout" , class: "username"

-else
  %span.blackbut
    = image_tag('exit.png')
    = link_to "Войти", "/signin", class: "exit"





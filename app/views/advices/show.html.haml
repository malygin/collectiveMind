%h2
  Обсуждение совета с автором
%section.widget.widget-tabs
  .body.tab-content
    .media.option_for_selectize{id: "post_#{@discontent_advice.id}", project: "#{@project.id}", post: "#{@discontent_advice.id}"}
      - if @discontent_advice.user
        = render 'users/user_post', user: @discontent_advice.user
      .media-body
        - if @discontent_advice.user
          .media-heading
            = link_to @discontent_advice.user, user_path(@project, @discontent_advice.user)
        %br
    = @discontent_advice.content

    %br
    %br
    %ul.media-list.chat-messages#comments
      = render @discontent_advice.comments.main
    - if signed_in?
      %br
      = render partial: 'advice_comments/form'

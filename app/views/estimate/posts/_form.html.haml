= form_for @estimate_post do |f|
  %b Цель проекта
  %br= simple_format(@plan_post.goal)
  .field
    = f.label 'Общая оценка'
    = f.text_area :content
  %b Список решаемых задач
  %br
  %br
  - @triplet_estimates.each do |key,value|
    %b Задача:
    = simple_format(key.task)
    %br
    %b Ресурсы для задачи
    = simple_format(key.supply) 
    %br
    %b  Предположим, что предлагаемые для этой задачи ресурсы будут созданы. Какова вероятность (выберите ее для каждого из трех вариантов ниже), что они обеспечат разрешение данной задачи --  
    .field
      = f.label "-- 1) в полной мере?"
      = select_tag "op[#{key.id}][1]",   options_for_select([["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]], value.op1)
    .field
      = f.label "-- 2) в значительной мере?"
      = select_tag "op[#{key.id}][2]",   options_for_select([["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]], value.op2)
    .field
      = f.label "-- 3) в небольшой мере?"
      = select_tag "op[#{key.id}][3]",   options_for_select([["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]], value.op3)
    .field
      = f.label 'Обоснуйте свою оценку'
      = text_area_tag "op_text[#{key.id}]", value.op
    %br
    %b Функционирование ресурсов, которые будут обеспечивать разрешение данной задачи в будущем, потребует  материальных и трудовых затрат. Какова вероятность (выберите ее для каждого из трех вариантов ниже), что потребуются--
    .field
      = f.label '--1) большие затраты'
      = select_tag "ozf[#{key.id}][1]",   options_for_select([["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]], value.ozf1)
    .field
      = f.label "-- 2) средние  затраты"
      = select_tag "ozf[#{key.id}][2]",   options_for_select([["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]], value.ozf2)
    .field
      = f.label "-- 3) малые затраты"
      = select_tag "ozf[#{key.id}][3]",   options_for_select([["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]], value.ozf3)
    .field
      = f.label 'Обоснуйте свою оценку'
      = text_area_tag "ozf_text[#{key.id}]", value.ozf
    %br
    %b Cредства создания ресурсов для задачи        
    = simple_format(key.howto)
    %br
    

    %b Для создания ресурсов разрешения данной задачи в проекте предлагаются вышеперечисленные средства, методы и кадры. Какова вероятность (выберите ее для каждого из трех вариантов ниже), что ресурсы будут действительно созданы --

    .field
      = f.label "-- 1) в полном объеме"
      = select_tag "on[#{key.id}][1]",   options_for_select([["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]], value.on1)
    .field
      = f.label "-- 2) в значительном объеме"
      = select_tag "on[#{key.id}][2]",   options_for_select([["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]], value.on2)
    .field
      = f.label "-- 3) в небольшом объеме"
      = select_tag "on[#{key.id}][3]",   options_for_select([["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]], value.on3)
    .field
      = f.label 'Обоснуйте свою оценку'
      = text_area_tag "on_text[#{key.id}]", value.on

    %br
    %b Использование вышеозначенных средств, методов и кадров создания ресурсов потребует  материальных и трудовых затрат. Какова вероятность (выберите ее для каждого из трех вариантов ниже), что потребуются-- 
    .field
      = f.label '--1) большие затраты'
      = select_tag "ozs[#{key.id}][1]",   options_for_select([["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]], value.ozs1)
    .field
      = f.label "--2) средние затраты"
      = select_tag "ozs[#{key.id}][2]",   options_for_select([["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]], value.ozs2)
    .field
      = f.label "--3) малые затраты"
      = select_tag "ozs[#{key.id}][3]",   options_for_select([["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]], value.ozs3)
    .field
      = f.label 'Обоснуйте свою оценку'
      = text_area_tag "ozs_text[#{key.id}]", value.ozs
    
    =image_tag('dots.png')
    %br


  %p
    %b Первый шаг
    %br= simple_format(@plan_post.first_step)
  %b Выше описана та часть новых ресурсов будущего ДО СГУ, которую планируется создать и запустить в работу на первом шаге осуществления проекта. Какова вероятность (выберите ее для каждого из трех вариантов ниже), что уже сразу после создания их работа улучшит ДО --
  .field
    = f.label "-- 1) очень ощутимо?"
    = f.select :oppsh1, [["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]]
  .field
    = f.label "-- 2) ощутимо?"
    = f.select :oppsh2, [["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]]
  .field
    = f.label "-- 3) не очень ощутимо"
    = f.select :oppsh3, [["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]]
  .field
    = f.label 'Обоснуйте свою оценку'
    = f.text_area :oppsh
  %br
  %b После того, как на первом шаге будет создана эта часть новых ресурсов, их функционирование в ДО потребует материальных и трудовых затрат. Какова вероятность (выберите ее для каждого из трех вариантов ниже), что потребуются --
  .field
    = f.label '-- 1) большие затраты'
    = f.select :ozpshf1, [["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]]
  .field
    = f.label "-- 2) средние затраты"
    = f.select :ozpshf2, [["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]]
  .field
    = f.label "-- 3) малые затраты"
    = f.select :ozpshf3, [["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]]
  .field
    = f.label 'Обоснуйте свою оценку'
    = f.text_area :ozpshf
  %br
  %b Для создания на первом шаге данной части новых ресурсов ДО в проекте предлагаются определенные средства, методы и кадры. Какова вероятность (выберите ее для каждого из трех вариантов ниже), что при помощи этих средств, методов и кадров планируемые ресурсы будут действительно созданы --
  .field
    = f.label "-- 1) в полном объеме"
    = f.select :onpsh1, [["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]]
  .field
    = f.label "-- 2) в значительном объеме"
    = f.select :onpsh2, [["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]]
  .field
    = f.label "-- 3) в небольшом объеме"
    = f.select :onpsh3, [["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]]
  .field
    = f.label 'Обоснуйте свою оценку'
    = f.text_area :onpsh

  %br
  %b Использование на первом шаге средств, методов и кадров для создания вышеозначенной части новых ресурсов потребует материальных и трудовых затрат. Какова вероятность (выберите ее для каждого из трех вариантов ниже), что потребуются -- 
  .field
    = f.label '--1) большие затраты'
    = f.select :ozpshs1, [["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]]
  .field
    = f.label "--2) средние затраты"
    = f.select :ozpshs2, [["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]]
  .field
    = f.label "--3) малые затраты"
    = f.select :ozpshs3, [["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]]
  .field
    = f.label 'Обоснуйте свою оценку'
    = f.text_area :ozpshs
  %br

  %br
  %b При осуществлении проекта возможны негативные явления. Какова веростояность (выберите ее для каждого из четырех вариантов ниже), что они будут-- 
  .field
    = f.label '-- 1) большие'
    = f.select :nepr1, [["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]]
  .field
    = f.label "-- 2) средние"
    = f.select :nepr2, [["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]]
  .field
    = f.label "-- 3) малые"
    = f.select :nepr3, [["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]]  
  .field
    = f.label "-- 4) никакие"
    = f.select :nepr4, [["не очень вероятно", 1], ["вероятно", 2], ["очень вероятно", 3]]
  .field
    = f.label 'Перечислите возможные негативные явления и обоснуйте свою оценку'
    = f.text_area :nepr
  %br
  = hidden_field_tag "post_id", @plan_post.id

  

  .actions
    = f.submit ''

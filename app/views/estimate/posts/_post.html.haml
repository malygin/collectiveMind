- if post.status!=2 and post.status!=3
    .media
        .pull-left
            %h1.text-success=post.score
        = render 'users/user_post', :user => post.user
        .media-body
            = trim_string(post.content)
            = link_to "Подробнее", {:action => "show", :project => @project, :id => post.id}, class: "more"
            -#-else
            -#	%p
            -#		= post.goal
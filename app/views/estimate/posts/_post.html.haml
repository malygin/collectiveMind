- if post.status!=2 and post.status!=3
    .wholePost.margin30{:class => ('gray' if post.status==0)}
        %h1=post.score
        = render 'users/user_post', :user => post.user
        .posts
            -if post.status==0
                Оценка выполнена не по требованиям
                %br
            = render 'users/user_name', :user => post.user, :created_at => post.created_at
            %b=estimate_trans_project(post.all_grade)
            %br

            %p
                = trim_string(post.content)
                = link_to "Подробнее", {:action => "show", :project => @project, :id => post.id}, class: "more"
            -#-else
            -#	%p
            -#		= post.goal
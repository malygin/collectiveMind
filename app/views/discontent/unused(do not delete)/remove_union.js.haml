$("#post_#{@union_post.id}").animate({height: 0, opacity:0}, 500, function() { $(this).remove(); });
$('#li_id_#{@union_post.id}').remove();
-if @post.one_last_post?
  $("#url_post_#{@post.discontent_posts.first.id}").html('<a rel="nofollow" data-method="put" href="/project/#{@project.id}/discontent/posts/#{@post.id}/remove_union?post_id=#{@post.discontent_posts.first.id}"><div class="btn btn-xs btn-danger">#{t('link.remove')}</div></a>');

.col-md-6.discontent-block{id: "discontent_post_#{post_short.id}", class: "#{post_aspect_classes(post_short)}", data: {id: "id-#{post_short.id}", date: "#{post_short.created_at.to_datetime.to_f}",
                            comment: "#{post_short.last_comment.present? ? post_short.last_comment.created_at.to_datetime.to_f : 0}"}}
  .post.post_type_2
    .post-header.color_me{"data-me-action" => "background-color", "data-me-color" => "1", "data-me-type" => "imperf"}
    .post-body.no_post_head.ch_action{"data-for" => "post_ch#{post_short.id}"}
      .media
        .media-left.text-center.gradient.post-poster
          = image_tag 'no-ava.png', class: "img-circle img50"
          %p.poster-name
            =post_short.user
          %p.time
            = Russian::strftime(post_short.created_at,'%d.%m.%Y')
        .media-body
          %table.table
            %tbody
              %tr.what
                %td.col-md-12.left-column.bold
                  = link_to({action: :show, project: @project, id: post_short.id}, remote: true, id: "show_record_#{post_short.id}") do
                    = trim_string(post_short.content, size = 50)
                    %i.fa.fa-angle-right
              %tr
                %td.col-md-12.left-column
                  = trim_string(post_short.what, size = 50)

          .post_cont_hover.white{id: "post_ch#{post_short.id}"}
            .wrapper.ps_cont.half_wheel_speed
              .post_cont_inner
                -post_short.comments.preview.each do |comment|
                  .comment_preview
                    %span.bold.com_date
                      = Russian::strftime(comment.created_at,'%d.%m.%Y')
                    %span.bold
                      ="#{comment.user}:"
                    =comment.content

    .post-bottom.white
      .row
        .col-md-9.col_padding_right_3.themes_cont
          .post-theme{id: "stripe#{post_short.id}"}
            - post_short.post_aspects.each_with_index do |aspect, index|
              %span.tag-stripes.color_me{class: "#{index == 0 ? 'active' : ''}", style: "background-color: #{aspect.color};"}
                %span.tags
                  = aspect.content
        .col-md-3.col_padding_left_3
          .text-right.small_icons
            %span.popup2.comments_icon{"data-for" => "post_ch#{post_short.id}"}
              = post_short.comments.count
              %i.fa.fa-comment-o
            = link_to({controller: post_short.class.name.underscore.pluralize, action: :like, project: @project, id: post_short.id, against: false}, remote: true, method: :put, id: "like_post_#{post_short.id}") do
              %span.popup2
                %small(id="lk_post_#{post_short.id}")
                  = post_short.users_pro.count
                %i.fa.fa-thumbs-o-up.fa-flip-horizontal
            = link_to({controller: post_short.class.name.underscore.pluralize, action: :like, project: @project,  id: post_short.id, against: true}, remote: true, method: :put, id: "dislike_post_#{post_short.id}") do
              %span.popup2
                %small(id="dlk_post_#{post_short.id}")
                  = post_short.users_against.count
                %i.fa.fa-thumbs-o-down.fa-flip-horizontal
            -if post_short.notes.present?
              %span.popup2
                %i.fa.fa-exclamation-triangle.font_lighter_grey









-#.col-md-4.discontent-block{id: "discontent_post_#{post_short.id}", class: "#{post_aspect_classes(post_short)}",
-#  data: {created: "#{(post_short.created_at.to_datetime.to_f)}", popular: "#{post_short.users_pro.count + (post_short.number_views * 0.3)}"}}
-#  = link_to({action: :show, project: @project, id: post_short.id, asp: params[:asp]}, remote: true, id: 'show_record',
-#  class: "imperfection lined-block #{post_short.style == 1 ? 'imperf-pale-pink' : 'imperf-pale-blue'}" ) do
-#    %table
-#      %tr
-#        - if post_short.notes.present?
-#          %td.imperf-left-column.admin-notice
-#            %span.admin-notice-sign !
-#            .admin-notice-displayable
-#              - post_short.notes.each do |note|
-#                %li
-#                  = note.content
-#                %br
-#              -#Замечание модератора
-#              .close-admin-button
-#        - else
-#          %td.imperf-left-column
-#
-#        %td.imperf-right-column
-#          .aspects-indexes
-#            - post_short.post_aspects.each do |asp|
-#              .circle-tag.back-color{style: "background-color:#{asp.color};"}
-#                = asp.short_name.present? ? asp.short_name : asp.content[0,2].mb_chars.downcase
-#      %tr
-#        %td.imperf-left-column.what-cell
-#          Что
-#        %td.imperf-right-column
-#          = post_short.content
-#      %tr
-#        %td.imperf-left-column
-#          Где
-#        %td.imperf-right-column
-#          = post_short.whered
-#      %tr
-#        %td.imperf-left-column
-#          Когда
-#        %td.imperf-right-column
-#          = post_short.whend
-#      %tr
-#        %td.imperf-left-column
-#          .imperf-bottom-line
-#        %td.imperf-right-column
-#          .imperf-bottom-line
-#            .info-stripe
-#              %span.popup-date
-#                = Russian::strftime(post_short.created_at,'%d %B %k:%M:%S')
-#              -#= link_to({controller: post_short.class.name.underscore.pluralize, action: :show_comments, project: @project, id: post_short.id, remote: true}) do
-#              %span.popup-comments
-#                = post_short.comments.count
-#                %i.fa.fa-comment-o
-#              = link_to({controller: post_short.class.name.underscore.pluralize ,action: :like, project: @project, id: post_short.id, against: false,  remote: true},method: :put, id: "plus_post_#{post_short.id}") do
-#                %span.popup-likes
-#                  %span(id="plus_counter_#{post_short.id}" style="margin-right:0;")
-#                    = post_short.users_pro.count
-#                  %i.fa.fa-thumbs-o-up
-#              = link_to({controller: post_short.class.name.underscore.pluralize ,action: :like, project: @project,  id: post_short.id, against: true, remote: true}, method: :put, id: "minus_post_#{post_short.id}") do
-#                %span.popup-dislikes
-#                  %span(id="minus_counter_#{post_short.id}" style="margin-right:0;")
-#                    = post_short.users_against.count
-#                  %i.fa.fa-thumbs-o-down

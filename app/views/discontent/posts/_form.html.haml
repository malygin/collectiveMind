-#=render partial: 'shared/help/new_discontent'
-#%button.btn.btn-info(data-toggle="modal" data-target="#modalNewDiscontent")
-#  Показать пример
-#%br
-#%br
= form_for  @post, :url => {:project => @project.id, :replace_id => @replace_post.nil? ? nil : @replace_post.id , :action => @post.persisted? ? 'update' : 'create'},  :remote => true, :html => { :id => 'new_discontent' }do |f|
  -if @post.errors.any?
    #error_explanation
      %h2= "#{pluralize(@discontent_post.errors.count, "error")} prohibited this discontent_post from being saved:"
      %ul
        - @post.errors.full_messages.each do |msg|
          %li= msg
  .row
    .col-md-12
      .text-center
        .h5
          Что вас не устраивает?
        %br

  .row
    .col-md-6
      .text-right
        %i
          %b Примеры:
          %br
          %span.label.label-danger  Имеющиеся недостатки
          %br
          %span.label.label-danger что
          Пыльный и отвратительный на вид большой пустырь
          %br
          %span.label.label-success  Отсутствующие достоинства
          %br
          %span.label.label-success что
          Отсутствие декоративных растений
    .col-md-6

      .form-group

        =f.text_area :content, class: 'form-control', rows: 5, placeholder: 'Отрицательное действие, явление, установление, черта', onkeyup: "activate_button(this);"
  .row
    .col-md-12
      .text-center
        .h5

          Где это происходит?
        %br
  .row
    .col-md-6
      .text-right
        %i
          %b Примеры:
          %br
          %span.label.label-danger где
          во дворе между домами
          %br
          %span.label.label-success где
          в вестибюлях и подъездах

    .col-md-6
      =f.autocomplete_field :whered, autocomplete_discontent_post_whered_discontent_posts_path, :"min-length" => 0, style: 'height: 44px', class: 'form-control autocomplete',placeholder: 'Место, организация, общность, в которой это происходит '
      %br
      %br
  .row
    .col-md-12
      .text-center
        .h5
          Когда это происходит?
        %br
  .row
    .col-md-6
      .text-right
        %i
          %b Примеры:
          %br
          %span.label.label-danger когда
          в теплое время года
          %br
          %span.label.label-success когда
          всегда
    .col-md-6
      =f.autocomplete_field :whend, autocomplete_discontent_post_whend_discontent_posts_path, :"min-length" => 0, style: 'height: 44px',  class: 'form-control autocomplete',placeholder: 'Во время какого события, мероприятия, процесса, ситуации '


  .row
    .col-md-6
      %br
      %h5.text-right
        %b К какой теме относится  несовершенство?
      %br
      %br
    .com-md-6
      %br
      = select_tag 'select_for_aspects', options_from_collection_for_select(@aspects.minus_view(@aspects_for_post.nil? ? [] : @aspects_for_post.pluck(:id)), "id", "content"),
       :prompt => 'Выберите тему' , style:'margin-left:15px; margin-top:10px; height: 30px;'
      %br
      #add_post_aspects(style="margin-left: 52%;margin-top: 10px;")
        -unless @aspects_for_post.nil?
          - @aspects_for_post.each do |ap|
            %div{id: "aspect_#{ap.id}"}
              %input{name: "discontent_post_aspects[]", type: "hidden", value: "#{ap.id}"}/
              %span.glyphicon.glyphicon-remove.text-danger.pull-left{onclick: "remove_discontent_aspect('#{ap.id}','#{ap.content}');", style: "cursor:pointer;text-decoration:none;font-size:15px;"}
              %span.span_aspect.label.label-t{id: "#{ap.id}"}=ap.content
      %br

  .row
    .col-md-12

      .form-group.text-center
        %b  Выберите тип несовершенства
        %label.radio-inline
          = f.radio_button :style,  '1'
          %span.text-danger Имеющиеся недостатки
        %label.radio-inline
          = f.radio_button :style,  '0'
          %span.text-success Отсутствующие достоинства

  %br
  %button#send_post.btn.btn-primary(type="submit" onclick='disontent_form_submit();'){:class => ('disabled' unless @post.content?)} Сохранить
  %hr
  %span#discontent_success.text-success Готово!
  %span#redirect_success

  %p#discontent_warning.text-primary
    Проверьте в списке ниже, не было ли ваше несовершенство или похожее на него добавлено другими участниками. Если да, то прекратите работу с ним.  Если вы уверены, что ваше несовершенство оригинально,  нажмите на кнопку "Все равно сохранить".
    Учтите,  модератор может рассудить, что на самом деле оно не оригинально, и вычесть у вас баллы.
  #posts

.pull-right
  %a.active.btn.btn-default.menu_table(href="#")
    %span.glyphicon.glyphicon-th-list
    Список
  %a.btn.btn-default.menu_table(href="/project/#{@project.id}/discontent/posts?view=table")
    %span.glyphicon.glyphicon-th
    Таблица
- if signed_in? and @project.status == 3
  = link_to '+ Добавить несовершенство', new_discontent_post_path(@project) , id: 'add_record',  class: 'btn btn-sm btn-success'
  %br
%br
- if @project.status == 4
  %h5 Модераторы сейчас занимаются обработкой несовершенств, объединяя их. Пока вы можете работать на сборе информации или обсуждая предложенные несовершенства
- if @project.status == 5
  %h5
    Модераторы объединили все поданные несовершенства в блоки, рассудите, пожалуйста,
    1) нет ли в каком то из блоков того, что к нему не относится по смыслу
    2) есть ли что-то, что разумно было бы отнести к определенному блоку, но этого не сделано?

- if can_union_discontents?(@project)
  %ul.nav.nav-tabs#tab_posts
    %li#origin.active
      =link_to "Исходные #{@project.discontent_ongoing_post.count}", "/project/#{@project.id}/discontent/posts/", remote: true
    %li#new
      =link_to "Объединные #{@project.discontent_accepted_post.count}", "/project/#{@project.id}/discontent/posts/unions", remote: true
    -#%li
    -#  %a(href="#part_origin" data-toggle="tab") Частично совпадающие (140)
  %br
  %br
  #posts
    = render  @posts


-else
  %small#filter_posts
    = render 'shared/filter_posts', project: @project.id, folder: @folder
  #posts
    = render  @posts
#pagination
  = render 'shared/pagination'
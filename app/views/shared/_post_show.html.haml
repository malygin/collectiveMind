
.wholePost
  = render 'users/user_post', :user => post.user
  .posts
    = render 'users/user_name', :user => post.user, :created_at => post.created_at
    %p
      %br=simple_format(post.show_content)
      -if post.class.method_defined? :style

        Тип:
        %b=discontent_style_name(post.style)
        - unless post.aspect.nil?
          %br
          Фрагмент:
          %b=post.aspect.content
        %br
        %br

        %b Переопределяемые несовершенства:
        -post.post_replaced.each do |rp|
          %div{:id => "dis_#{rp.id}"}
            =rp.show_content.html_safe
            %br


   
    = render 'shared/post_social_bar', :post => post

= render 'shared/post_edit_bar', :post => post


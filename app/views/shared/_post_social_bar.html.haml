.social 
	- if signed_in? and not post.users.include?(current_user)
		=link_to "#{post.users.count}",  {:controller => post.class.name.underscore.pluralize ,:action => :plus, :project => @project,:status=>0, :aspect=>0, :id => post.id}, :id => post.id,:remote => true, :method => :put, class: "counter"
	-else
		=post.users.count
	=image_tag "like.png"
	=post.number_views
	=image_tag "views.png"
	=post.comments.count
	=image_tag "comment.png"
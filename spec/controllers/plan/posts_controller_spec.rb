require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe Plan::PostsController do

  # This should return the minimal set of attributes required to create a valid
  # Plan::Post. As you add validations to Plan::Post, be sure to
  # update the return value of this method accordingly.
  def valid_attributes
    {}
  end

  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # Plan::PostsController. Be sure to keep this updated too.
  def valid_session
    {}
  end

  describe "GET index" do
    it "assigns all plan_posts as @plan_posts" do
      post = Plan::Post.create! valid_attributes
      get :index, {}, valid_session
      assigns(:plan_posts).should eq([post])
    end
  end

  describe "GET show" do
    it "assigns the requested plan_post as @plan_post" do
      post = Plan::Post.create! valid_attributes
      get :show, {:id => post.to_param}, valid_session
      assigns(:plan_post).should eq(post)
    end
  end

  describe "GET new" do
    it "assigns a new plan_post as @plan_post" do
      get :new, {}, valid_session
      assigns(:plan_post).should be_a_new(Plan::Post)
    end
  end

  describe "GET edit" do
    it "assigns the requested plan_post as @plan_post" do
      post = Plan::Post.create! valid_attributes
      get :edit, {:id => post.to_param}, valid_session
      assigns(:plan_post).should eq(post)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new Plan::Post" do
        expect {
          post :create, {:plan_post => valid_attributes}, valid_session
        }.to change(Plan::Post, :count).by(1)
      end

      it "assigns a newly created plan_post as @plan_post" do
        post :create, {:plan_post => valid_attributes}, valid_session
        assigns(:plan_post).should be_a(Plan::Post)
        assigns(:plan_post).should be_persisted
      end

      it "redirects to the created plan_post" do
        post :create, {:plan_post => valid_attributes}, valid_session
        response.should redirect_to(Plan::Post.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved plan_post as @plan_post" do
        # Trigger the behavior that occurs when invalid params are submitted
        Plan::Post.any_instance.stub(:save).and_return(false)
        post :create, {:plan_post => {}}, valid_session
        assigns(:plan_post).should be_a_new(Plan::Post)
      end

      it "re-renders the 'new' template" do
        # Trigger the behavior that occurs when invalid params are submitted
        Plan::Post.any_instance.stub(:save).and_return(false)
        post :create, {:plan_post => {}}, valid_session
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested plan_post" do
        post = Plan::Post.create! valid_attributes
        # Assuming there are no other plan_posts in the database, this
        # specifies that the Plan::Post created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        Plan::Post.any_instance.should_receive(:update_attributes).with({'these' => 'params'})
        put :update, {:id => post.to_param, :plan_post => {'these' => 'params'}}, valid_session
      end

      it "assigns the requested plan_post as @plan_post" do
        post = Plan::Post.create! valid_attributes
        put :update, {:id => post.to_param, :plan_post => valid_attributes}, valid_session
        assigns(:plan_post).should eq(post)
      end

      it "redirects to the plan_post" do
        post = Plan::Post.create! valid_attributes
        put :update, {:id => post.to_param, :plan_post => valid_attributes}, valid_session
        response.should redirect_to(post)
      end
    end

    describe "with invalid params" do
      it "assigns the plan_post as @plan_post" do
        post = Plan::Post.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        Plan::Post.any_instance.stub(:save).and_return(false)
        put :update, {:id => post.to_param, :plan_post => {}}, valid_session
        assigns(:plan_post).should eq(post)
      end

      it "re-renders the 'edit' template" do
        post = Plan::Post.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        Plan::Post.any_instance.stub(:save).and_return(false)
        put :update, {:id => post.to_param, :plan_post => {}}, valid_session
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested plan_post" do
      post = Plan::Post.create! valid_attributes
      expect {
        delete :destroy, {:id => post.to_param}, valid_session
      }.to change(Plan::Post, :count).by(-1)
    end

    it "redirects to the plan_posts list" do
      post = Plan::Post.create! valid_attributes
      delete :destroy, {:id => post.to_param}, valid_session
      response.should redirect_to(plan_posts_url)
    end
  end

end
